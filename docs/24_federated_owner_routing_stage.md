# Federated Owner Routing Stage

Статус документа:

- compatibility-stub для архивного historical snapshot;
- активная архивная копия перенесена в `docs/archive/24_federated_owner_routing_stage.md`;
- этот путь оставлен временно, чтобы не ломать старые ссылки и чужие заметки.

Что делать теперь:

1. Для активного reading order возвращаться в `docs/README.md`.
2. Для historical context открывать `docs/archive/24_federated_owner_routing_stage.md`.
3. Для текущей routing truth использовать `04_sync_and_ownership.md`, `05_ui_shell_and_navigation.md`, `27_platform_shell_v1_spec.md` и `40_platform_shell_navigation_alignment.md`.
