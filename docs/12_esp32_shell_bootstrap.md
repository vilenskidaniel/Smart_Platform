# ESP32 Shell Bootstrap

Статус документа:

- compatibility-stub для архивного historical snapshot;
- активная архивная копия перенесена в `docs/archive/12_esp32_shell_bootstrap.md`;
- этот путь оставлен временно, чтобы не ломать старые ссылки и чужие заметки.

Что делать теперь:

1. Для активного reading order возвращаться в `docs/README.md`.
2. Для historical context открывать `docs/archive/12_esp32_shell_bootstrap.md`.
3. Для текущей shell truth использовать `26_v1_product_spec.md`, `05_ui_shell_and_navigation.md` и `27_platform_shell_v1_spec.md`.
