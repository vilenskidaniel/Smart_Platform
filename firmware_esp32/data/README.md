# data

Здесь будут храниться web-ресурсы, которые `ESP32` будет отдавать через `LittleFS`.

Пока это только заготовка.

Позже сюда войдут:

- shell-страницы;
- статические ресурсы интерфейса;
- service/test страницы модулей;
- fallback-страницы для деградации.
